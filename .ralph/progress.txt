# Ralph Progress Log
Started: Sun Mar 22 15:44:26 KST 2026

## Codebase Patterns
- All services use Go stdlib only (no external deps) with `go.mod` at service root
- Health endpoint pattern: `GET /healthz` → `{"status":"ok","service":"<name>"}`
- FFmpeg streams use `-c copy` (no transcoding) and `-f flv` to RTMP destination
- Docker images: `golang:1.22-alpine` builder + `alpine:3.19` runtime
- Config files mounted read-only from `config/` directory
- All services on `sentinel-net` internal Docker network
- Graceful shutdown: SIGTERM → wait → SIGKILL pattern for FFmpeg processes
- Exponential backoff with max 30s for reconnection loops

---

## 2026-03-22 - US-001
- Implemented youtube-adapter service: Go HTTP server + yt-dlp + FFmpeg
- Resolves YouTube URLs via `yt-dlp --get-url`, streams via FFmpeg to RTMP
- Auto-loops on video completion, exponential backoff on errors
- Health endpoint at `/healthz`, status endpoint at `/api/streams/status`
- Files changed: services/youtube-adapter/{Dockerfile,go.mod,main.go}, config/youtube-sources.json, docker-compose.yml, .ralph/verify.sh
- **Learnings for future iterations:**
  - youtube-adapter follows same pattern as cctv-adapter but uses yt-dlp for URL resolution instead of direct RTSP
  - `-re` flag needed in FFmpeg for YouTube (reads at native frame rate, unlike RTSP which is real-time)
  - Config uses {id, youtubeUrl, streamKey} format at /config/youtube-sources.json
  - Added youtube-adapter to verify.sh service list for go vet checks
---
